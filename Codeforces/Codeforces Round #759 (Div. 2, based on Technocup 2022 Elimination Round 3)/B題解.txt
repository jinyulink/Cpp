給定一個長度為n的數組a，x每次操作，會將a分為兩個數組，其中一個是小於等於a[n]的，另一個是大於a[n]的，順序和原數組保持一致，然後小的一半在左，大的在右合併為新的數組。經過若干次操作後，數組保持不變，問這個次數是多少？

題解：只有當a[n]是最大值的時候才會保持不變。

每一次操作，a[n]都變成從後到左的第一個大於a[n]的數。所以答案就是從最後位置往前的最長遞增子序列長度。

考: DP的LIS Dynamic Programming Longest Increasing Subsequence
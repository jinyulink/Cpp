#include<bits/stdc++.h>
using namespace std;
#define ull unsigned long long
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        ull n,k;
        cin>>n>>k;
        vector<int> a(n);
        ull sum=0;
        for(int i=0;i<n;i++)
        {
            cin>>a[i];
            sum+=a[i];
        }
        
    }
    return 0;
}